# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET         /report                         controllers.Vote.getReport
POST        /event                          controllers.Vote.handleEvent()